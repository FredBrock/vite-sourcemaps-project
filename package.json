{
  "name": "vite-sourcemaps-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm -r --filter ./packages/* dev",
    "build": "pnpm -r --filter ./packages/* build",
    "preview": "pnpm -r --filter ./packages/* preview",
    "debug": "pnpm run build && pnpm run preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
